import { useState } from "react";

const SearchField = ({search}: {search: () => {}}) => {
  const [searchField, setSearchField] = useState();
  const [searchValue, setSearcValue] = useState();

  
}

export default SearchField;